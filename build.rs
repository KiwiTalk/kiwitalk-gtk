/*extern crate fs_extra;

use std::env;
use std::path::{Path, PathBuf};
use std::str::FromStr;
use fs_extra::dir::CopyOptions;*/

fn main() {
	/*println!("cargo:rerun-if-changed=src/gui/assets");
	let out_dir = PathBuf::from_str(env::var_os("OUT_DIR").unwrap().to_str().unwrap()).unwrap();
	let assets = PathBuf::from_str("src/gui/assets").unwrap();
	fs_extra::dir::copy(assets, out_dir, &CopyOptions::default()).unwrap();
	//TODO fix*/
}